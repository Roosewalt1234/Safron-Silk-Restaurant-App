# Copy this file to `.env.local` (recommended) and set your Gemini API key.
# Note: This app currently uses the key in the browser, so it will be visible to users.

GEMINI_API_KEY=YOUR_GEMINI_API_KEY_HERE

